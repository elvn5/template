interface:
  display_name: "Template"
  short_description: "Repo-specific patterns and workflows for template"
  default_prompt: "Use the template repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true